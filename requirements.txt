# Flask framework and extensions
Flask==2.0.1
Flask-SQLAlchemy==2.5.1
Flask-WTF==0.14.3
Flask-Login==0.5.0
Flask-JWT-Extended==4.3.1

# Database drivers
psycopg2-binary==2.9.1  # PostgreSQL driver
pymongo==3.12.0        # MongoDB driver (optional, if using MongoDB)

# Natural Language Processing (NLP) libraries
spacy==3.1.1
nltk==3.6.2
transformers==4.8.2    # Optional, if using Hugging Face Transformers

# Machine Learning libraries (optional)
scikit-learn==0.24.2
tensorflow==2.5.0      # Optional, if using TensorFlow

# Charting libraries
matplotlib==3.4.2      # For generating charts (optional)
chart.js               # For client-side chart rendering (include via CDN)

# Other utilities
bcrypt==3.2.0          # For password hashing
requests==2.25.1       # For making HTTP requests (if needed)
gunicorn==20.1.0       # WSGI server for production deployment
python-dotenv==0.19.0  # For loading environment variables from .env files

# Bootstrap and other front-end libraries (optional, include via CDN)
# bootstrap==5.0.2      # Include via CDN in HTML templates
# jquery==3.6.0         # Include via CDN in HTML templates
# popper.js==1.16.1     # Include via CDN in HTML templates